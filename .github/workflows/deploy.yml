name: Deploy

on:
  push:
    branches: [main]

jobs:
  # ── Run checks and tests before deploying ─────────────────────────────────────
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install all workspace deps
        run: npm ci

      - name: Astro type check
        run: npm run check -w site

      - name: Worker tests
        run: npm test -w worker

  # ── Deploy Cloudflare Worker (runs in parallel with deploy-site) ───────────────
  deploy-worker:
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install all workspace deps
        run: npm ci

      - name: Deploy worker to Cloudflare
        run: npm run deploy -w worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  # ── Build Astro site and deploy static files to VPS ───────────────────────────
  deploy-site:
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install all workspace deps
        run: npm ci

      - name: Build site
        run: npm run build -w site
        env:
          SITE_URL: https://prabigya.com.np

      - name: Deploy static files to VPS
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "site/dist/"
          target: "/home/${{ secrets.VPS_USER }}/portfolio-kit"
          strip_components: 0
